
name: "Tests 3.14"

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  tests_3_14:
    uses: ./.github/workflows/tests-by-python.yml
    with:
      python-version: "3.14"

