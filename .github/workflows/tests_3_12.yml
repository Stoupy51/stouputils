
name: "Tests 3.12"

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  tests_3_12:
    uses: ./.github/workflows/tests-by-python.yml
    with:
      python-version: "3.12"

